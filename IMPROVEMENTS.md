# 🔧 Улучшения алгоритма определения домена

## ✅ Что было улучшено:

### 1. **Извлечение домена из названия архива** (САМЫЙ ВЫСОКИЙ ПРИОРИТЕТ!)
Теперь система в первую очередь пытается извлечь домен из названия архива:
- `example.com.zip` → `example.com`
- `mysite.net_backup.rar` → `mysite.net`
- `site-example-com.zip` → `example.com`
- `example_com_2024.zip` → `example.com`

Автоматически убираются:
- Суффиксы: `_backup`, `-archive`, `_site`, `_www`
- Даты: `2024`, `20240102`
- Расширения: `.zip`, `.rar`, `.7z`

### 2. **Приоритетные паттерны в файлах** (вес 70-100)
Если домен не найден в названии архива, система ищет его в специальных местах:
- `<base href="">` - самый приоритетный (вес 100)
- `<link rel="canonical">` - каноническая ссылка (вес 90)
- `<meta property="og:url">` - Open Graph URL (вес 80)
- WordPress конфиги: `site_url`, `WP_HOME`, `WP_SITEURL` (вес 70)

### 3. **Взвешенный подсчет по типам файлов**
Разные файлы имеют разный приоритет:
- HTML/PHP файлы: вес **3** (самые важные)
- XML/конфиги (.conf, .htaccess, .env): вес **2** (важные)
- CSS/JS/JSON: вес **1** (менее приоритетные)

### 4. **Нормализация поддоменов**
Система автоматически убирает поддомены:
- `www.example.com` → `example.com`
- `shop.example.com` → `example.com`
- `blog.site.co.uk` → `site.co.uk` (поддержка двухуровневых зон)

### 5. **Расширенная фильтрация CDN и сторонних сервисов**
Добавлено 40+ доменов которые игнорируются:
- Google (Analytics, Ads, Fonts, Maps)
- Социальные сети (Facebook, Twitter, Instagram, VK, OK)
- CDN (Cloudflare, jsDelivr, CDNJS, Bootstrap CDN)
- Метрики (Yandex.Metrika, Hotjar, CrazyEgg)
- И другие популярные сервисы

### 6. **Алгоритм выбора домена**

```
1. Сканируем все файлы
2. В HTML/PHP ищем приоритетные паттерны
3. Если нашли - выбираем с наивысшим приоритетом
4. Если не нашли - считаем частоту с учетом:
   - Веса типа файла
   - Нормализации поддоменов
   - Фильтрации CDN
5. Возвращаем самый частый домен
```

## 📊 Результат:

**До:** Мог найти `cdn.jsdelivr.net`, `fonts.google.com` или поддомен `blog.site.com`

**После:** Точно определяет основной домен сайта `site.com` на основе:
- Canonical URL из HTML
- WordPress конфигов
- Частоты упоминаний с весами
- Нормализации поддоменов

## 🎯 Точность:

- ✅ Поддержка WordPress, Joomla, Drupal
- ✅ Распознавание canonical и og:url
- ✅ Фильтрация 40+ популярных CDN
- ✅ Правильная обработка поддоменов
- ✅ Взвешенный подсчет по типам файлов

---

Эти улучшения значительно повышают точность определения основного домена сайта! 🚀
